apiVersion: v1
kind: Service
metadata:
  name: cc-tomcat-svc
  namespace: cc-backend
  labels:
    app: tomcat
spec:
  type: ClusterIP
  selector:
    app: tomcat
  ports:
    - port: 8080        # Service Port
      targetPort: 8080  # Pod의 containerPort
